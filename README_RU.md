# Security Scanner API - Веб-сканер уязвимостей

Полнофункциональное веб-приложение для обнаружения уязвимостей в веб-приложениях: **XSS (межсайтовый скриптинг)** и **SQL-инъекции (SQLi)**. Построено на FastAPI с современным интерфейсом на Bootstrap 5.

---

## 🎯 Основные возможности

### Публичный интерфейс
- **Сканирование URL**: Проверка URL на наличие потенциальных уязвимостей безопасности
- **Сканирование текста**: Анализ текстовых данных на XSS и SQL-инъекции
- **Результаты в реальном времени**: Мгновенная обратная связь с детальными отчётами об уязвимостях
- **Оценка риска**: Цветовая индикация уровней риска (Безопасно, Низкий, Средний, Высокий)
- **Детальные результаты**: Просмотр обнаруженных паттернов с указанием уровня опасности

### Админ-панель (`/admin-panel`)
- **Аутентификация**: Безопасный вход на основе JWT
- **История сканирований**: Просмотр всех прошлых сканирований с подробной информацией
- **Статистика**: Мониторинг активности сканирования и обнаружения угроз
- **Управление правилами**: Динамическое включение/отключение правил сканирования
- **Системные логи**: Просмотр логов приложения для отладки и мониторинга

---

## 🏗️ Архитектура

Приложение следует принципам **объектно-ориентированного программирования (ООП)** и лучшим практикам **SOLID/DRY**:

```
Security_Scanner_API/
├── admin/              # Аутентификация и авторизация админа
│   └── auth.py
├── api/                # Обработчики маршрутов FastAPI
│   ├── public_routes.py
│   └── admin_routes.py
├── scanner/            # Движок обнаружения уязвимостей
│   └── vulnerability_scanner.py
├── storage/            # Управление базой данных (SQLite)
│   └── database.py
├── validators/         # Валидация и санитизация входных данных
│   └── input_validator.py
├── templates/          # HTML шаблоны
│   ├── index.html
│   └── admin.html
├── main.py            # Точка входа приложения
├── app.py             # Flask демо-приложение
└── requirements.txt   # Python зависимости
```

---

## 🚀 Установка

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов)

### Шаги установки

1. **Клонируйте репозиторий**:
   ```bash
   git clone <repository-url>
   cd Security_Scanner_API
   ```

2. **Создайте виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите приложение**:
   ```bash
   python main.py
   ```

   Или используя uvicorn напрямую:
   ```bash
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
   ```

5. **Откройте приложение**:
   - Основной сканер: http://localhost:8000
   - Автоматическая документация: http://localhost:8000/docs (откроется автоматически)
   - Админ-панель: http://localhost:8000/admin-panel
   - API документация: http://localhost:8000/docs

---

## 📝 Учетные данные администратора

**Для входа в админ-панель используйте следующие учетные данные:**

- **Логин (Username)**: `admin`
- **Пароль (Password)**: `admin123`

**URL админ-панели**: http://localhost:8000/admin-panel

⚠️ **Важно**: Обязательно смените пароль администратора перед использованием в продакшене!

**Как изменить пароль**:
1. Используйте скрипт `generate_password_hash.py` для генерации нового хеша пароля
2. Обновите пароль в базе данных или создайте нового пользователя

---

## 🌐 Запуск демо-версии через браузер

Для удобной демонстрации через браузер используйте Flask приложение:

### Быстрый старт:

1. **Запустите основной FastAPI сервер** (в первом терминале):
   ```bash
   python main.py
   ```

2. **Запустите демо-приложение Flask** (во втором терминале):
   ```bash
   python app.py
   ```

3. **Откройте в браузере**:
   - Сканер: http://localhost:5000/scanner
   - Админ-панель: http://localhost:5000/admin-panel

📖 **Подробные инструкции**: см. файл `DEMO_GUIDE.md`

---

## 🔧 Конфигурация

### База данных
Приложение использует SQLite по умолчанию. Файл базы данных (`security_scanner.db`) будет создан автоматически при первом запуске.

### Настройки безопасности
- Секретный ключ JWT задан в `admin/auth.py` (измените в продакшене!)
- Время жизни токена: 30 минут
- Хеширование паролей: bcrypt (в демо-режиме используется простой текст)

---

## 📊 API Endpoints

### Публичные endpoints

- `POST /api/scan` - Сканирование входных данных на уязвимости
  ```json
  {
    "input_data": "http://example.com?q=<script>alert('xss')</script>",
    "scan_type": "url"
  }
  ```

- `GET /api/health` - Проверка работоспособности
- `GET /scanner` - Интерфейс сканера
- `GET /` - Перенаправление на `/docs` (автоматическая документация)

### Админ endpoints (требуется аутентификация)

- `POST /api/admin/login` - Аутентификация администратора
- `GET /api/admin/scan-history` - Получить историю сканирований
- `GET /api/admin/statistics` - Получить статистику приложения
- `GET /api/admin/rules` - Получить правила сканирования
- `PUT /api/admin/rules/{id}` - Обновить правило сканирования
- `GET /api/admin/logs` - Получить системные логи
- `GET /admin-panel` - Интерфейс админ-панели

---

## 🛡️ Функции безопасности

### Валидация входных данных
- Проверка формата URL
- Ограничение длины текста
- Санитизация входных данных для безопасного хранения
- HTML-экранирование для предотвращения XSS в сохранённых данных

### Обнаружение уязвимостей

#### Паттерны обнаружения XSS:
- Обнаружение тегов `<script>`
- JavaScript протокол (`javascript:`)
- Обработчики событий (`onclick`, `onerror`, и т.д.)
- Теги iframe
- SVG со встроенными скриптами
- И многое другое...

#### Паттерны обнаружения SQL-инъекций:
- UNION SELECT запросы
- Булевые SQL-инъекции
- SQL команды (DROP, DELETE, и т.д.)
- SQL разделители комментариев (`--`, `/* */`)
- SQL функции и процедуры
- И многое другое...

---

## 🎨 Особенности интерфейса

### Основной интерфейс
- Современный адаптивный дизайн на Bootstrap 5
- Градиентные фоны и плавные анимации
- Цветовая индикация рисков
- Детальные отчёты об уязвимостях
- Интерактивные карточки результатов

### Админ-панель
- Тёмная тема для профессионального внешнего вида
- Панель статистики с визуальными карточками
- Сортируемые и фильтруемые таблицы
- Управление правилами в реальном времени
- Комплексный интерфейс логирования

---

## 🔒 Лучшие практики безопасности

1. **Санитизация входных данных**: Все пользовательские входные данные санитизируются перед сохранением
2. **Хеширование паролей**: Bcrypt для безопасного хранения паролей
3. **JWT аутентификация**: Безопасная токен-ориентированная аутентификация администратора
4. **Предотвращение SQL-инъекций**: Параметризованные запросы и валидация входных данных
5. **Предотвращение XSS**: HTML-экранирование и меры безопасности контента

---

## 📈 Будущие улучшения

- Поддержка дополнительных типов уязвимостей (CSRF, XXE, и т.д.)
- Экспорт отчётов сканирования (PDF, JSON, CSV)
- Email уведомления для находок с высоким риском
- Создание пользовательских правил через админ-панель
- Ограничение скорости API
- Управление ролями пользователей
- Планирование сканирований

---

## 📚 Дополнительная документация

- **Быстрый старт**: `QUICKSTART.md` - Краткое руководство по запуску
- **Демо-руководство**: `DEMO_GUIDE.md` - Инструкции по запуску демо-версии через браузер
- **API документация**: http://localhost:8000/docs - Автоматическая интерактивная документация FastAPI

---

## 🤝 Вклад в проект

Вклад приветствуется! Пожалуйста, не стесняйтесь отправлять Pull Request.

---

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл LICENSE для деталей.

---

## ⚠️ Отказ от ответственности

Этот инструмент предназначен **только для образовательных целей и авторизованного тестирования**. Не используйте этот инструмент для сканирования систем без явного разрешения. Несанкционированное сканирование может быть незаконным в вашей юрисдикции.

---

## 🆘 Устранение неполадок

### Проблема: Порт 8000 уже занят

**Решение:**
```bash
# Windows - найти процесс
netstat -ano | findstr :8000

# Остановить процесс
taskkill /PID <PID> /F

# Или измените порт в main.py
```

### Проблема: Ошибка "Template not found"

**Решение:**
- Убедитесь, что папка `templates/` существует в корне проекта
- Проверьте права доступа к файлам

### Проблема: Не работает аутентификация

**Решение:**
- Убедитесь, что база данных создана (запустите `python main.py`)
- Проверьте логи в файле `app.log`
- Используйте скрипт `create_admin_user.py` для пересоздания пользователя

---

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи в файле `app.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порты 8000 и 5000 свободны
4. Ознакомьтесь с документацией API на `/docs`

---

**Создано с ❤️ с использованием FastAPI и Bootstrap 5**

---

## 📖 Примеры использования

### Пример 1: Сканирование URL

```bash
curl -X POST "http://localhost:8000/api/scan" \
  -H "Content-Type: application/json" \
  -d '{
    "input_data": "http://example.com?q=<script>alert(1)</script>",
    "scan_type": "url"
  }'
```

### Пример 2: Сканирование текста

```bash
curl -X POST "http://localhost:8000/api/scan" \
  -H "Content-Type: application/json" \
  -d '{
    "input_data": "' OR '1'='1",
    "scan_type": "text"
  }'
```

### Пример 3: Вход в админ-панель

```bash
curl -X POST "http://localhost:8000/api/admin/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

---

**Приятного использования!** 🚀

