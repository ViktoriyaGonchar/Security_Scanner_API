# 🌐 Руководство по запуску демо-приложения

Это руководство поможет вам запустить демонстрационную версию Security Scanner API через браузер.

---

## 📋 Предварительные требования

1. **Python 3.8+** установлен
2. **Все зависимости установлены** (см. `requirements.txt`)
3. **FastAPI сервер запущен** (основное приложение)

---

## 🚀 Быстрый старт

### Шаг 1: Установите дополнительные зависимости для Flask

Демо-приложение использует Flask. Установите его:

```bash
pip install flask requests
```

Или добавьте в `requirements.txt`:
```
flask>=2.3.0
requests>=2.31.0
```

Затем установите:
```bash
pip install -r requirements.txt
```

---

### Шаг 2: Запустите основной FastAPI сервер

**В первом терминале/окне:**

```bash
python main.py
```

Вы должны увидеть:
```
============================================================
🚀 Security Scanner API Starting...
============================================================
📍 API URL: http://localhost:8000
📚 Documentation: http://localhost:8000/docs
...
```

✅ **Убедитесь, что сервер запущен на порту 8000**

---

### Шаг 3: Запустите демо-приложение Flask

**Во втором терминале/окне:**

```bash
python app.py
```

Вы должны увидеть:
```
============================================================
🌐 Security Scanner API - Demo Application
============================================================
📍 Demo URL: http://localhost:5000
🔍 Scanner: http://localhost:5000/scanner
🔐 Admin Panel: http://localhost:5000/admin-panel
============================================================
```

---

## 🌍 Доступ к приложению через браузер

### Откройте в браузере:

1. **Главная страница (автоматический редирект):**
   - URL: `http://localhost:5000`
   - Автоматически перенаправит на `/scanner`

2. **Интерфейс сканера:**
   - URL: `http://localhost:5000/scanner`
   - Форма для сканирования URL и текста на уязвимости

3. **Админ-панель:**
   - URL: `http://localhost:5000/admin-panel`
   - Логин: `admin`
   - Пароль: `admin123`

---

## 📖 Использование

### 1. Сканирование URL

1. Откройте `http://localhost:5000/scanner`
2. Выберите тип "URL"
3. Введите URL для проверки, например:
   ```
   http://example.com?q=<script>alert('XSS')</script>
   ```
4. Нажмите "Start Scan"
5. Просмотрите результаты сканирования

### 2. Сканирование текста

1. Откройте `http://localhost:5000/scanner`
2. Выберите тип "Text"
3. Введите текст для проверки, например:
   ```
   ' OR '1'='1
   ```
4. Нажмите "Start Scan"
5. Просмотрите результаты сканирования

### 3. Админ-панель

1. Откройте `http://localhost:5000/admin-panel`
2. Введите учетные данные:
   - **Логин:** `admin`
   - **Пароль:** `admin123`
3. Нажмите "Login"
4. Используйте функции админ-панели:
   - Статистика
   - История сканирований
   - Управление правилами
   - Системные логи

---

## 🔧 Настройка

### Изменение порта FastAPI сервера

Если ваш FastAPI сервер запущен на другом порту, отредактируйте `app.py`:

```python
# Измените эту строку
API_BASE_URL = "http://localhost:8000"  # Укажите ваш порт
```

### Изменение порта демо-приложения

Если порт 5000 занят, измените в `app.py`:

```python
app.run(
    host='0.0.0.0',
    port=5000,  # Измените на нужный порт
    debug=True
)
```

---

## ⚠️ Устранение неполадок

### Ошибка: "Cannot connect to API server"

**Проблема:** Демо-приложение не может подключиться к FastAPI серверу.

**Решение:**
1. Убедитесь, что FastAPI сервер запущен (`python main.py`)
2. Проверьте, что сервер работает на `http://localhost:8000`
3. Откройте в браузере `http://localhost:8000/docs` - должна открыться документация
4. Проверьте переменную `API_BASE_URL` в `app.py`

---

### Ошибка: "Port 5000 is already in use"

**Проблема:** Порт 5000 уже занят другим приложением.

**Решение:**
1. Измените порт в `app.py` (см. раздел "Настройка")
2. Или остановите процесс, занимающий порт:
   ```bash
   # Windows
   netstat -ano | findstr :5000
   taskkill /PID <PID> /F
   ```

---

### Ошибка: "Template not found"

**Проблема:** Flask не может найти шаблоны.

**Решение:**
1. Убедитесь, что папка `templates/` находится в той же директории, что и `app.py`
2. Структура должна быть:
   ```
   Security_Scanner_API/
   ├── app.py
   ├── templates/
   │   ├── index.html
   │   └── admin.html
   ```

---

## 🎯 Примеры тестовых данных

### XSS тесты:
```
<script>alert('XSS')</script>
javascript:alert('XSS')
<img src=x onerror=alert('XSS')>
<iframe src="javascript:alert('XSS')"></iframe>
```

### SQL Injection тесты:
```
' OR '1'='1
1' UNION SELECT * FROM users--
'; DROP TABLE users;--
admin'--
' OR 1=1--
```

---

## 📝 Примечания

- Демо-приложение работает как **прокси** между браузером и FastAPI сервером
- Все данные обрабатываются основным FastAPI сервером
- Демо-приложение только отображает интерфейс и передает запросы

---

## 🔗 Полезные ссылки

- **FastAPI документация:** http://localhost:8000/docs
- **API информация:** http://localhost:8000/api
- **Демо приложение:** http://localhost:5000

---

## ✅ Готово!

Теперь вы можете использовать демо-приложение через браузер для тестирования Security Scanner API.

**Приятного использования!** 🚀

