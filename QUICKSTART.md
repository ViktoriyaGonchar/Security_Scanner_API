# Quick Start Guide

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск приложения

```bash
python main.py
```

Или с помощью uvicorn:

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Доступ к приложению

- **Главная страница**: http://localhost:8000
- **Админ-панель**: http://localhost:8000/admin
- **API документация**: http://localhost:8000/docs

### 4. Вход в админ-панель

- **Логин**: `admin`
- **Пароль**: `admin123`

⚠️ **Важно**: Измените пароль администратора перед использованием в продакшене!

### 5. Использование

#### Сканирование URL:
1. Выберите тип "URL"
2. Введите URL для проверки (например: `http://example.com?q=<script>alert('xss')</script>`)
3. Нажмите "Start Scan"

#### Сканирование текста:
1. Выберите тип "Text"
2. Введите текст для проверки
3. Нажмите "Start Scan"

### 6. Примеры тестовых данных

**XSS тесты:**
- `<script>alert('XSS')</script>`
- `javascript:alert('XSS')`
- `<img src=x onerror=alert('XSS')>`

**SQL Injection тесты:**
- `' OR '1'='1`
- `1' UNION SELECT * FROM users--`
- `'; DROP TABLE users;--`

### 7. Генерация нового хеша пароля

Если нужно изменить пароль администратора:

```bash
python generate_password_hash.py
```

Затем обновите хеш в базе данных или создайте нового пользователя через SQL.

---

Приятного использования! 🚀

